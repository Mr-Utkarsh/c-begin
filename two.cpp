#include <iostream>
using namespace std;

int main() {
    int a = 0;
    int b = 8;
    // int b = 3;
    // int c = 9;
    
    // arithmetic operators

    // cout<<"the ans of a+b is = "<<a+b;
    // cout<<endl;
    // cout<<"the ans of a-b is = "<<a-b;
    // cout<<endl;
    // cout<<"the ans of a*b is = "<<a*b;
    // cout<<endl;
    // cout<<"the ans of a/b is = "<<a/b;
    // cout<<endl;
    // cout<<"the ans of a%b is = "<<a%b;
    // cout<<endl;

    // relational operators 
    cout<<"the ans of a<b is = "<<(a<b);
    cout<<endl;
    cout<<"the ans of a>b is = "<<(a>b);
    cout<<endl;
    cout<<"the ans of a<=b is = "<<(a<=b);
    cout<<endl;
    cout<<"the ans of a>=b is = "<<(a>=b);
    cout<<endl;
    cout<<"the ans of a!=b is = "<<(a!=b);
    cout<<endl;
    cout<<"the ans of a==b is = "<<(a==b);
    cout<<endl;

    // Assignment operators
    // cout<<"the ans of a+=b is = "<<(a+=b);
    // cout<<endl;
    // cout<<"the ans of a-=b is = "<<(a-=b);
    // cout<<endl;
    // cout<<"the ans of a*=b is = "<<(a*=b);
    // cout<<endl;
    // cout<<"the ans of a/=b is = "<<(a/=b);
    // cout<<endl;
    // cout<<"the ans of a%=b is = "<<(a%=b);
    // cout<<endl;

    //Logical Operators
    
    //Bitwise Operator
    // and&
    // or||
    // not!

    //Ternary Operator
    // (a>b)?cout<<"a is greater":cout<<"b is smaller";
    // (a%2==0)?cout<<"a is even":cout<<"a is odd";
    // ((a%2==0) && (a%3==0))?cout<<" a is divisible by both":cout<<"a is not divisible by both ";
    // (a>=10 && a<=20)?(a%2==0 || a%3==0)?cout<<"divisible":cout<<"not divisible":cout<<"invalid";

    // int c;
    // c=a||b;
    // cout<<c<<endl;
    // c=a&&b;
    // cout<<c<<endl;;
    // cout<<!c;
}